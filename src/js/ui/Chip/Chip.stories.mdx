import Chip from './Chip';
import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import Avatar from '../Avatar';
import {SvgUse} from '../SvgIcon';


<Meta title="Components/Chips" component={Chip} />

# Chip

Chips are compact elements that allow users to enter information, select a choice, filter content, or trigger an action.

<Preview>
  <Story name="default">
    <Chip color="default" label="default"/>
  </Story>
  <Story name="primary">
    <Chip color="primary" label="primary"/>
  </Story>
  <Story name="secondary">
    <Chip color="secondary" label="secondary"/>
  </Story>
  <Story name="accent">
    <Chip color="accent" label="accent"/>
  </Story>
</Preview>

1. Thumbnail (optional)
Thumbnails identify entities (like individuals) by displaying an avatar, logo, or icon.
2. Remove icon [optional]
Entry chips can include a Remove icon.

<Preview>
  <Story name="avatar">
    <Chip
      avatar={<Avatar><SvgUse name='facets' className="text-white"/></Avatar>}
      label="avatar"
      onDelete={e => {}}
    />
  </Story>
</Preview>


<Props of={Chip} />
